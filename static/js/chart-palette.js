/**
 * Chart palette constants.
 *
 * Leaf module with no side effects, so plain-data modules like `state.js` can
 * import it without pulling in `dom.js`.
 */

// Slots in the chart palette: the --chart-1…8 tokens in variables.css.
export const CHART_PALETTE_SIZE = 8;
